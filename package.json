{
  "name": "dev",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "JZIB 校园墙",
  "scripts": {
    "dev": "wrangler dev",
    "dev:worker": "wrangler dev --env dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env dev",
    "deploy:prod": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply jzib_points --local",
    "d1:dev:migrate": "wrangler d1 migrations apply DB --env dev --remote",
    "d1:remote:migrate": "wrangler d1 migrations apply jzib_points --remote",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "hono": "^4.7.10"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@cloudflare/workers-types": "^4.20260520.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.93.0"
  }
}
